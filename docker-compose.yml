version: "3.9"
services:
  dev-db:
    image: postgres:14
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: unbiliever13
      POSTGRES_DB: test_db

    networks:
      - freecodecamp

networks:
  freecodecamp:
